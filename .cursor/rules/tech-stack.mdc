---
description: 技术栈约束：规定本项目使用的框架、库和工具版本，防止 AI 引入错误的替代方案
alwaysApply: true
---

# 技术栈约束

## 包管理器
使用 **Bun**，禁止生成 `npm install` / `pnpm install` 命令。依赖安装统一用 `bun add`。

## Next.js
- 版本 **16（App Router）**，禁止使用 Pages Router API
- 禁止：`getServerSideProps` / `getStaticProps` / `getInitialProps` / `pages/` 目录
- Server Components 默认无 `'use client'`，需要交互时才加

## UI 与样式
- 组件库：**shadcn/ui**，需要新组件时优先从 shadcn 添加，不引入 MUI / Ant Design 等
- 样式：**Tailwind CSS v4** 语法（`@import "tailwindcss"` 而非 `tailwind.config.js`）
- 禁止内联 `style={{}}` 对象，一律用 Tailwind 类名

## 状态管理
- 使用 **Zustand v5**，禁止引入 Redux / Jotai / Recoil
- 每次订阅必须传 selector，禁止 `useStore()` 无参订阅

## 图表
- 使用 **Lightweight Charts v5**，禁止引入 ECharts / Recharts / Chart.js
- 更新数据用 `series.update()` 增量接口，不整体 setData

## 精度计算
- 价格、数量等金融数值用 **bignumber.js**
- 热路径（订单簿行渲染）允许用原生 `toFixed` 避免 GC

## 虚拟列表
- 使用 **@tanstack/react-virtual v3**，禁止 react-window / react-virtualized

## 国际化
- 使用 **next-intl v4**，语言切换通过 cookie，不修改 URL 路径
